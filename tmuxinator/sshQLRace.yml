# ~/.tmuxinator/sshQLRace.yml

name: sshQLRace
root: ~/
tmux_options: -2

windows:
  - QLRace Servers:
      layout: tiled
      panes:
        - tmux setw synchronize-panes on && ssh de.qlrace.com
        - ssh il.qlrace.com
        - ssh au.qlrace.com
        - ssh c16t.uk
